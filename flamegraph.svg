<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="374" onload="init(evt)" viewBox="0 0 1200 374" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:"Verdana"; font-size:12px; fill:rgb(0,0,0); }
#title { text-anchor:middle; font-size:17px; }
#search { opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[var nametype = 'Function:';
var fontsize = 12;
var fontwidth = 0.59;
var xpad = 10;
var inverted = false;
var searchcolor = 'rgb(230,0,230)';
var fluiddrawing = true;
var truncate_text_right = false;]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
          svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            var el = frames.children;
            for(var i = 0; i < el.length; i++) {
                update_text(el[i]);
            }

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad - 100;
            matchedtxt.attributes.x.value = svgWidth - xpad - 100;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
            var params = get_params()
            params.x = el.attributes._orig_x.value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["_orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("_orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["_orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["_orig_" + attr].value;
    e.removeAttribute("_orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));
    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (/^ *\$/.test(txt) || t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.attributes != undefined) {
        orig_load(e, "x");
        orig_load(e, "width");
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, ratio) {
    if (e.attributes != undefined) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = format_percent((parseFloat(e.attributes.x.value) - x) * ratio);
            if (e.tagName == "text") {
                e.attributes.x.value = format_percent(parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value) + (100 * 3 / frames.attributes.width.value));
            }
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = format_percent(parseFloat(e.attributes.width.value) * ratio);
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, ratio);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseFloat(attr.width.value);
    var xmin = parseFloat(attr.x.value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    var ratio = 100 / width;
    // XXX: Workaround for JavaScript float issues (fix me)
    var fudge = 0.001;
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseFloat(a.x.value);
        var ew = parseFloat(a.width.value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                update_text(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex + fudge >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, ratio);
                update_text(e);
            }
        }
    }
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
        update_text(el[i]);
    }
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseFloat(rect.attributes.width.value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseFloat(rect.attributes.x.value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    var fudge = 0.0001;    // JavaScript floating point
    for (var k in keys) {
        var x = parseFloat(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw - fudge) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="374" fill="url(#background)"/><text id="title" x="50.0000%" y="24.00">Flame Graph</text><text id="details" x="10" y="357.00"> </text><text id="unzoom" class="hide" x="10" y="24.00">Reset Zoom</text><text id="search" x="1090" y="24.00">Search</text><text id="matched" x="1090" y="357.00"> </text><svg id="frames" x="10" width="1180"><g><title>libsystem_kernel.dylib`madvise (726 samples, 0.39%)</title><rect x="0.0266%" y="181" width="0.3943%" height="15" fill="rgb(227,0,7)"/><text x="0.2766%" y="191.50"></text></g><g><title>libsystem_malloc.dylib`free_medium (848 samples, 0.46%)</title><rect x="0.0261%" y="197" width="0.4606%" height="15" fill="rgb(217,0,24)"/><text x="0.2761%" y="207.50"></text></g><g><title>libsystem_malloc.dylib`medium_madvise_free_range_no_lock (119 samples, 0.06%)</title><rect x="0.4220%" y="181" width="0.0646%" height="15" fill="rgb(221,193,54)"/><text x="0.6720%" y="191.50"></text></g><g><title>libsystem_kernel.dylib`madvise (119 samples, 0.06%)</title><rect x="0.4220%" y="165" width="0.0646%" height="15" fill="rgb(248,212,6)"/><text x="0.6720%" y="175.50"></text></g><g><title>libsystem_malloc.dylib`mvm_deallocate_pages (1,426 samples, 0.77%)</title><rect x="0.4878%" y="197" width="0.7745%" height="15" fill="rgb(208,68,35)"/><text x="0.7378%" y="207.50"></text></g><g><title>libsystem_kernel.dylib`_kernelrpc_mach_vm_deallocate_trap (1,426 samples, 0.77%)</title><rect x="0.4878%" y="181" width="0.7745%" height="15" fill="rgb(232,128,0)"/><text x="0.7378%" y="191.50"></text></g><g><title>rnn`&lt;T as core::convert::TryInto&lt;U&gt;&gt;::try_into (185 samples, 0.10%)</title><rect x="1.2623%" y="181" width="0.1005%" height="15" fill="rgb(207,160,47)"/><text x="1.5123%" y="191.50"></text></g><g><title>libsystem_platform.dylib`_platform_memmove$VARIANT$Haswell (175 samples, 0.10%)</title><rect x="1.2677%" y="165" width="0.0951%" height="15" fill="rgb(228,23,34)"/><text x="1.5177%" y="175.50"></text></g><g><title>rnn`&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter (39 samples, 0.02%)</title><rect x="1.3628%" y="181" width="0.0212%" height="15" fill="rgb(218,30,26)"/><text x="1.6128%" y="191.50"></text></g><g><title>rnn`&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter (39 samples, 0.02%)</title><rect x="1.3628%" y="165" width="0.0212%" height="15" fill="rgb(220,122,19)"/><text x="1.6128%" y="175.50"></text></g><g><title>libsystem_kernel.dylib`vm_copy (452 samples, 0.25%)</title><rect x="87.9148%" y="69" width="0.2455%" height="15" fill="rgb(250,228,42)"/><text x="88.1648%" y="79.50"></text></g><g><title>libsystem_kernel.dylib`mach_msg_trap (452 samples, 0.25%)</title><rect x="87.9148%" y="53" width="0.2455%" height="15" fill="rgb(240,193,28)"/><text x="88.1648%" y="63.50"></text></g><g><title>libsystem_malloc.dylib`free_large (1,524 samples, 0.83%)</title><rect x="88.1603%" y="69" width="0.8278%" height="15" fill="rgb(216,20,37)"/><text x="88.4103%" y="79.50"></text></g><g><title>libsystem_malloc.dylib`mvm_deallocate_pages (1,524 samples, 0.83%)</title><rect x="88.1603%" y="53" width="0.8278%" height="15" fill="rgb(206,188,39)"/><text x="88.4103%" y="63.50"></text></g><g><title>libsystem_kernel.dylib`_kernelrpc_mach_vm_deallocate_trap (1,524 samples, 0.83%)</title><rect x="88.1603%" y="37" width="0.8278%" height="15" fill="rgb(217,207,13)"/><text x="88.4103%" y="47.50"></text></g><g><title>libsystem_malloc.dylib`large_try_realloc_in_place (35 samples, 0.02%)</title><rect x="88.9886%" y="69" width="0.0190%" height="15" fill="rgb(231,73,38)"/><text x="89.2386%" y="79.50"></text></g><g><title>libsystem_kernel.dylib`vm_allocate (35 samples, 0.02%)</title><rect x="88.9886%" y="53" width="0.0190%" height="15" fill="rgb(225,20,46)"/><text x="89.2386%" y="63.50"></text></g><g><title>libsystem_kernel.dylib`_kernelrpc_mach_vm_allocate_trap (35 samples, 0.02%)</title><rect x="88.9886%" y="37" width="0.0190%" height="15" fill="rgb(210,31,41)"/><text x="89.2386%" y="47.50"></text></g><g><title>libsystem_malloc.dylib`large_malloc (21 samples, 0.01%)</title><rect x="89.0087%" y="53" width="0.0114%" height="15" fill="rgb(221,200,47)"/><text x="89.2587%" y="63.50"></text></g><g><title>libsystem_malloc.dylib`mvm_allocate_pages (21 samples, 0.01%)</title><rect x="89.0087%" y="37" width="0.0114%" height="15" fill="rgb(226,26,5)"/><text x="89.2587%" y="47.50"></text></g><g><title>libsystem_malloc.dylib`szone_malloc_should_clear (26 samples, 0.01%)</title><rect x="89.0087%" y="69" width="0.0141%" height="15" fill="rgb(249,33,26)"/><text x="89.2587%" y="79.50"></text></g><g><title>libsystem_malloc.dylib`szone_realloc (2,887 samples, 1.57%)</title><rect x="87.9148%" y="85" width="1.5681%" height="15" fill="rgb(235,183,28)"/><text x="88.1648%" y="95.50"></text></g><g><title>libsystem_platform.dylib`_platform_memmove$VARIANT$Haswell (847 samples, 0.46%)</title><rect x="89.0228%" y="69" width="0.4601%" height="15" fill="rgb(221,5,38)"/><text x="89.2728%" y="79.50"></text></g><g><title>rnn`&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter (162,423 samples, 88.22%)</title><rect x="1.2623%" y="197" width="88.2211%" height="15" fill="rgb(247,18,42)"/><text x="1.5123%" y="207.50">rnn`&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter</text></g><g><title>rnn`&lt;core::iter::adapters::map::Map&lt;I,F&gt; as core::iter::traits::iterator::Iterator&gt;::fold (162,199 samples, 88.10%)</title><rect x="1.3840%" y="181" width="88.0994%" height="15" fill="rgb(241,131,45)"/><text x="1.6340%" y="191.50">rnn`&lt;core::iter::adapters::map::Map&lt;I,F&gt; as core::iter::traits::iterator::Iterator&gt;::fold</text></g><g><title>rnn`rnn::subtract_matrices (162,196 samples, 88.10%)</title><rect x="1.3856%" y="165" width="88.0978%" height="15" fill="rgb(249,31,29)"/><text x="1.6356%" y="175.50">rnn`rnn::subtract_matrices</text></g><g><title>rnn`alloc::raw_vec::RawVec&lt;T,A&gt;::reserve::do_reserve_and_handle (2,895 samples, 1.57%)</title><rect x="87.9110%" y="149" width="1.5724%" height="15" fill="rgb(225,111,53)"/><text x="88.1610%" y="159.50"></text></g><g><title>rnn`alloc::raw_vec::finish_grow (2,891 samples, 1.57%)</title><rect x="87.9131%" y="133" width="1.5703%" height="15" fill="rgb(238,160,17)"/><text x="88.1631%" y="143.50"></text></g><g><title>libsystem_malloc.dylib`realloc (2,891 samples, 1.57%)</title><rect x="87.9131%" y="117" width="1.5703%" height="15" fill="rgb(214,148,48)"/><text x="88.1631%" y="127.50"></text></g><g><title>libsystem_malloc.dylib`malloc_zone_realloc (2,890 samples, 1.57%)</title><rect x="87.9137%" y="101" width="1.5697%" height="15" fill="rgb(232,36,49)"/><text x="88.1637%" y="111.50"></text></g><g><title>libsystem_platform.dylib`_platform_bzero$VARIANT$Haswell (18,744 samples, 10.18%)</title><rect x="89.5078%" y="101" width="10.1809%" height="15" fill="rgb(209,103,24)"/><text x="89.7578%" y="111.50">libsystem_platf..</text></g><g><title>libsystem_malloc.dylib`medium_malloc_should_clear (18,760 samples, 10.19%)</title><rect x="89.5002%" y="117" width="10.1896%" height="15" fill="rgb(229,88,8)"/><text x="89.7502%" y="127.50">libsystem_mallo..</text></g><g><title>libsystem_malloc.dylib`_malloc_zone_calloc (18,766 samples, 10.19%)</title><rect x="89.4986%" y="149" width="10.1929%" height="15" fill="rgb(213,181,19)"/><text x="89.7486%" y="159.50">libsystem_mallo..</text></g><g><title>libsystem_malloc.dylib`szone_malloc_should_clear (18,764 samples, 10.19%)</title><rect x="89.4997%" y="133" width="10.1918%" height="15" fill="rgb(254,191,54)"/><text x="89.7497%" y="143.50">libsystem_mallo..</text></g><g><title>rnn`rnn::Network::nabla (18,780 samples, 10.20%)</title><rect x="89.4926%" y="197" width="10.2005%" height="15" fill="rgb(241,83,37)"/><text x="89.7426%" y="207.50">rnn`rnn::Networ..</text></g><g><title>rnn`&lt;core::iter::adapters::map::Map&lt;I,F&gt; as core::iter::traits::iterator::Iterator&gt;::fold (18,780 samples, 10.20%)</title><rect x="89.4926%" y="181" width="10.2005%" height="15" fill="rgb(233,36,39)"/><text x="89.7426%" y="191.50">rnn`&lt;core::iter..</text></g><g><title>rnn`&lt;alloc::vec::Vec&lt;T&gt; as alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt;&gt;::from_iter (18,775 samples, 10.20%)</title><rect x="89.4954%" y="165" width="10.1978%" height="15" fill="rgb(226,3,54)"/><text x="89.7454%" y="175.50">rnn`&lt;alloc::vec..</text></g><g><title>rnn`rnn::dot_product (20 samples, 0.01%)</title><rect x="99.6931%" y="197" width="0.0109%" height="15" fill="rgb(245,192,40)"/><text x="99.9431%" y="207.50"></text></g><g><title>rnn`alloc::vec::source_iter_marker::_&lt;impl alloc::vec::spec_from_iter::SpecFromIter&lt;T,I&gt; for alloc::vec::Vec&lt;T&gt;&gt;::from_iter (235 samples, 0.13%)</title><rect x="99.7154%" y="165" width="0.1276%" height="15" fill="rgb(238,167,29)"/><text x="99.9654%" y="175.50"></text></g><g><title>libsystem_platform.dylib`_platform_bzero$VARIANT$Haswell (32 samples, 0.02%)</title><rect x="99.8457%" y="133" width="0.0174%" height="15" fill="rgb(232,182,51)"/><text x="100.0957%" y="143.50"></text></g><g><title>libsystem_platform.dylib`_platform_memmove$VARIANT$Haswell (37 samples, 0.02%)</title><rect x="99.8631%" y="133" width="0.0201%" height="15" fill="rgb(231,60,39)"/><text x="100.1131%" y="143.50"></text></g><g><title>libsystem_platform.dylib`_platform_memmove$VARIANT$Haswell (21 samples, 0.01%)</title><rect x="99.9413%" y="53" width="0.0114%" height="15" fill="rgb(208,69,12)"/><text x="100.1913%" y="63.50"></text></g><g><title>rnn`&lt;flate2::gz::bufread::GzDecoder&lt;R&gt; as std::io::Read&gt;::read (168 samples, 0.09%)</title><rect x="99.8832%" y="133" width="0.0913%" height="15" fill="rgb(235,93,37)"/><text x="100.1332%" y="143.50"></text></g><g><title>rnn`flate2::zio::read (165 samples, 0.09%)</title><rect x="99.8849%" y="117" width="0.0896%" height="15" fill="rgb(213,116,39)"/><text x="100.1349%" y="127.50"></text></g><g><title>rnn`&lt;flate2::ffi::rust::Inflate as flate2::ffi::InflateBackend&gt;::decompress (158 samples, 0.09%)</title><rect x="99.8887%" y="101" width="0.0858%" height="15" fill="rgb(222,207,29)"/><text x="100.1387%" y="111.50"></text></g><g><title>rnn`miniz_oxide::inflate::stream::inflate (158 samples, 0.09%)</title><rect x="99.8887%" y="85" width="0.0858%" height="15" fill="rgb(206,96,30)"/><text x="100.1387%" y="95.50"></text></g><g><title>rnn`miniz_oxide::inflate::core::decompress (156 samples, 0.08%)</title><rect x="99.8897%" y="69" width="0.0847%" height="15" fill="rgb(218,138,4)"/><text x="100.1397%" y="79.50"></text></g><g><title>rnn`rnn::mnist_loader::load_all (500 samples, 0.27%)</title><rect x="99.7040%" y="197" width="0.2716%" height="15" fill="rgb(250,191,14)"/><text x="99.9540%" y="207.50"></text></g><g><title>rnn`rnn::mnist_loader::load_data (500 samples, 0.27%)</title><rect x="99.7040%" y="181" width="0.2716%" height="15" fill="rgb(239,60,40)"/><text x="99.9540%" y="191.50"></text></g><g><title>rnn`rnn::mnist_loader::MnistData::new (244 samples, 0.13%)</title><rect x="99.8430%" y="165" width="0.1325%" height="15" fill="rgb(206,27,48)"/><text x="100.0930%" y="175.50"></text></g><g><title>rnn`std::io::read_to_end (239 samples, 0.13%)</title><rect x="99.8457%" y="149" width="0.1298%" height="15" fill="rgb(225,35,8)"/><text x="100.0957%" y="159.50"></text></g><g><title>libsystem_malloc.dylib`malloc_zone_realloc (27 samples, 0.01%)</title><rect x="99.9848%" y="133" width="0.0147%" height="15" fill="rgb(250,213,24)"/><text x="100.2348%" y="143.50"></text></g><g><title>libsystem_malloc.dylib`szone_realloc (27 samples, 0.01%)</title><rect x="99.9848%" y="117" width="0.0147%" height="15" fill="rgb(247,123,22)"/><text x="100.2348%" y="127.50"></text></g><g><title>all (184,109 samples, 100%)</title><rect x="0.0000%" y="325" width="100.0000%" height="15" fill="rgb(231,138,38)"/><text x="0.2500%" y="335.50"></text></g><g><title>0x1 (184,109 samples, 100.00%)</title><rect x="0.0000%" y="309" width="100.0000%" height="15" fill="rgb(231,145,46)"/><text x="0.2500%" y="319.50">0x1</text></g><g><title>libdyld.dylib`start (184,109 samples, 100.00%)</title><rect x="0.0000%" y="293" width="100.0000%" height="15" fill="rgb(251,118,11)"/><text x="0.2500%" y="303.50">libdyld.dylib`start</text></g><g><title>rnn`main (184,109 samples, 100.00%)</title><rect x="0.0000%" y="277" width="100.0000%" height="15" fill="rgb(217,147,25)"/><text x="0.2500%" y="287.50">rnn`main</text></g><g><title>rnn`std::rt::lang_start_internal (184,109 samples, 100.00%)</title><rect x="0.0000%" y="261" width="100.0000%" height="15" fill="rgb(247,81,37)"/><text x="0.2500%" y="271.50">rnn`std::rt::lang_start_internal</text></g><g><title>rnn`std::rt::lang_start::_{{closure}} (184,108 samples, 100.00%)</title><rect x="0.0005%" y="245" width="99.9995%" height="15" fill="rgb(209,12,38)"/><text x="0.2505%" y="255.50">rnn`std::rt::lang_start::_{{closure}}</text></g><g><title>rnn`std::sys_common::backtrace::__rust_begin_short_backtrace (184,108 samples, 100.00%)</title><rect x="0.0005%" y="229" width="99.9995%" height="15" fill="rgb(227,1,9)"/><text x="0.2505%" y="239.50">rnn`std::sys_common::backtrace::__rust_begin_short_backtrace</text></g><g><title>rnn`rnn::main (184,108 samples, 100.00%)</title><rect x="0.0005%" y="213" width="99.9995%" height="15" fill="rgb(248,47,43)"/><text x="0.2505%" y="223.50">rnn`rnn::main</text></g><g><title>rnn`rnn::multiply_vectors4 (45 samples, 0.02%)</title><rect x="99.9756%" y="197" width="0.0244%" height="15" fill="rgb(221,10,30)"/><text x="100.2256%" y="207.50"></text></g><g><title>rnn`alloc::raw_vec::RawVec&lt;T,A&gt;::reserve::do_reserve_and_handle (30 samples, 0.02%)</title><rect x="99.9837%" y="181" width="0.0163%" height="15" fill="rgb(210,229,1)"/><text x="100.2337%" y="191.50"></text></g><g><title>rnn`alloc::raw_vec::finish_grow (30 samples, 0.02%)</title><rect x="99.9837%" y="165" width="0.0163%" height="15" fill="rgb(222,148,37)"/><text x="100.2337%" y="175.50"></text></g><g><title>libsystem_malloc.dylib`realloc (29 samples, 0.02%)</title><rect x="99.9842%" y="149" width="0.0158%" height="15" fill="rgb(234,67,33)"/><text x="100.2342%" y="159.50"></text></g></svg></svg>